config {
  type: "view",
  schema: "silver",
  description: "Cleaned and standardized location (delivery zone) data from client-manager-db"
}

SELECT
  CAST(Id AS INT64) AS id_location,
  CAST(Name AS STRING) AS name,
  CAST(Description AS STRING) AS description,
  CAST(Status AS INT64) AS status,
  CAST(Created AS TIMESTAMP) AS created_at,
  CAST(Updated AS TIMESTAMP) AS updated_at,
  CAST(UserId AS STRING) AS user_id,
  CAST(ModifiedByUserId AS STRING) AS modified_by_user_id
FROM ${ref("raw_client_manager_Location")}
